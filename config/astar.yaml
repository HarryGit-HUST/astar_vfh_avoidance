target_x: 6.0
target_y: 0.0
debug_mode: true

planner:
  takeoff_height: 1.2    # 起飞和巡航高度
  uav_radius: 0.3
  safe_margin: 0.3
  max_speed: 0.8
  min_safe_dist: 0.3
  
  lookahead_dist: 1.5
  astar_weight: 1.5
  replan_cooldown: 1.5
  map_decay_rate: 2      # 衰减慢一点，保持记忆